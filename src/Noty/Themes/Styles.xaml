<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- The deck is drawn on top of other people's windows, so its menus are built
       from scratch in the same dark, quiet palette rather than arriving as a strip
       of system chrome with a grey gutter down the side. -->

  <SolidColorBrush x:Key="MenuPaper" Color="#F51D1D21" />
  <SolidColorBrush x:Key="MenuEdge" Color="#2EFFFFFF" />
  <SolidColorBrush x:Key="MenuInk" Color="#F0FFFFFF" />
  <SolidColorBrush x:Key="MenuInkDim" Color="#8CFFFFFF" />
  <SolidColorBrush x:Key="MenuInkFaint" Color="#59FFFFFF" />
  <SolidColorBrush x:Key="MenuHighlight" Color="#1FFFFFFF" />
  <SolidColorBrush x:Key="MenuRule" Color="#1FFFFFFF" />

  <!-- The note editor lives on coloured paper and must not inherit the stock WPF
       scrollbar. The editor supplies NoteScrollThumbBrush from its active palette. -->
  <Style x:Key="NoteScrollThumb" TargetType="Thumb">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border x:Name="Grip"
                  Background="{Binding Foreground,
                                      RelativeSource={RelativeSource AncestorType={x:Type ScrollBar}}}"
                  CornerRadius="2.5"
                  Margin="1.5,2"
                  Opacity="0.62" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Grip" Property="Opacity" Value="0.86" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter TargetName="Grip" Property="Opacity" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="NoteScrollBar" TargetType="ScrollBar">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{DynamicResource NoteScrollThumbBrush}" />
    <Setter Property="Width" Value="7" />
    <Setter Property="MinWidth" Value="7" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid Background="Transparent" Width="7">
            <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageUpCommand"
                              Opacity="0" Focusable="False" IsTabStop="False" />
              </Track.DecreaseRepeatButton>
              <Track.Thumb>
                <Thumb Style="{StaticResource NoteScrollThumb}" />
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageDownCommand"
                              Opacity="0" Focusable="False" IsTabStop="False" />
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
              <Setter Property="Height" Value="7" />
              <Setter Property="Width" Value="Auto" />
              <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Shared by the context menu itself and by every submenu it opens. -->
  <Style x:Key="MenuSurface" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource MenuPaper}" />
    <Setter Property="BorderBrush" Value="{StaticResource MenuEdge}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="9" />
    <Setter Property="Padding" Value="5" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="Black" Opacity="0.45" BlurRadius="18"
                          ShadowDepth="3" Direction="270"
                          RenderingBias="Performance" />
      </Setter.Value>
    </Setter>
  </Style>

  <ControlTemplate x:Key="NotyMenuItem" TargetType="MenuItem">
    <Grid>
      <Border x:Name="Row"
              Background="Transparent"
              CornerRadius="6"
              Padding="0"
              SnapsToDevicePixels="True">
        <Grid Height="26">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <!-- The tick sits in its own narrow column rather than a raised chrome
               gutter, so an unchecked row reads as plain text. -->
          <TextBlock x:Name="Tick" Grid.Column="0"
                     Text="&#x2713;" FontSize="11"
                     Foreground="{StaticResource MenuInk}"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="Collapsed" />

          <ContentPresenter x:Name="Header" Grid.Column="1"
                            ContentSource="Header"
                            RecognizesAccessKey="True"
                            VerticalAlignment="Center"
                            Margin="0,0,18,0" />

          <TextBlock x:Name="Arrow" Grid.Column="2"
                     Text="&#x203A;" FontSize="14"
                     Foreground="{StaticResource MenuInkFaint}"
                     Margin="0,0,8,2"
                     VerticalAlignment="Center"
                     Visibility="Collapsed" />
        </Grid>
      </Border>

      <Popup x:Name="PART_Popup"
             AllowsTransparency="True"
             Focusable="False"
             IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
             Placement="Right"
             HorizontalOffset="-6"
             VerticalOffset="-6"
             PopupAnimation="Fade">
        <Border Style="{StaticResource MenuSurface}" Margin="8">
          <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
        </Border>
      </Popup>
    </Grid>

    <ControlTemplate.Triggers>
      <Trigger Property="Role" Value="SubmenuHeader">
        <Setter TargetName="Arrow" Property="Visibility" Value="Visible" />
      </Trigger>
      <Trigger Property="IsChecked" Value="True">
        <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
      </Trigger>
      <Trigger Property="IsHighlighted" Value="True">
        <Setter TargetName="Row" Property="Background" Value="{StaticResource MenuHighlight}" />
      </Trigger>
      <Trigger Property="IsSubmenuOpen" Value="True">
        <Setter TargetName="Row" Property="Background" Value="{StaticResource MenuHighlight}" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter TargetName="Header" Property="Opacity" Value="0.4" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <Style TargetType="MenuItem">
    <Setter Property="Foreground" Value="{StaticResource MenuInk}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Template" Value="{StaticResource NotyMenuItem}" />
  </Style>

  <Style TargetType="ContextMenu">
    <Setter Property="Foreground" Value="{StaticResource MenuInk}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="12.5" />
    <!-- Transparency for the popup window itself, which is what lets the rounded
         corners be rounded instead of sitting on a white square. -->
    <Setter Property="HasDropShadow" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContextMenu">
          <Border Style="{StaticResource MenuSurface}" Margin="8">
            <ItemsPresenter SnapsToDevicePixels="True" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Separators inside a menu take this key, not the plain Separator style. -->
  <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Separator">
          <Border Height="1" Margin="10,5,10,5"
                  Background="{StaticResource MenuRule}"
                  SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ToolTip">
    <Setter Property="Foreground" Value="{StaticResource MenuInk}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="HasDropShadow" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToolTip">
          <Border Background="{StaticResource MenuPaper}"
                  BorderBrush="{StaticResource MenuEdge}"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="8,4,8,5"
                  Margin="6">
            <ContentPresenter />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
